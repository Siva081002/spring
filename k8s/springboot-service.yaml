apiVersion: v1
kind: Service
metadata:
  name: springboot-service
  namespace: sivareddy
spec:
  selector:
    app: springboot
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
